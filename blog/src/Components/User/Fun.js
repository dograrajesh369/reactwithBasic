import React from "react";

function Func() {
  
    return (
        <div>test of button function inside the same function
            <button onClick={() => alert("Hello")}>Hit Me</button>
        </div>

    )
}
export default Func;